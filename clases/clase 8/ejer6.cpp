/*Dado un lote valores provistos por el usuario, que finalizan con 0, definir y cargar un vector de 15 posiciones (de manera ordenada) y calcular: a. La sumatoria de todos los valores luego de ingresarlos al vector b. El valor máximo c. El valor mínimo d. Generar un segundo vector que contenga sólo la mitad de cada valor cargado en el vector original e. Genere un tercer vector con el doble de cada valor cargado en el vector original*/

#include <iostream>
using namespace std;

int main (){

    int vector[15];
    int len;

    return 0;
}